{
  "name": "videochat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js",
    "start": "concurrently --kill-others-on-fail \"set HTTPS=true&&nodemon server.js\" \"cd frontend && yarn start\"",
    "build": "concurrently --kill-others-on-fail \" nodemon server.js\" \"cd api &&  http-server build -p 3000  \""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "nodemon": "^2.0.7",
    "socket.io": "^3.1.1"
  },
  "devDependencies": {
    "concurrently": "^6.2.1"
  }
}
